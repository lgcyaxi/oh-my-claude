export { createFormatters, type Colors, type Formatters } from "./colors";
export { INSTALL_DIR, CLAUDE_DIR, PROXY_SCRIPT, PID_FILE, SETTINGS_PATH, CONFIG_PATH, SESSIONS_DIR } from "./paths";
export { checkHealth } from "./health";
export { ensureProxyRunning } from "./proxy-lifecycle";
